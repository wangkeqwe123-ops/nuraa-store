import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
export function ConversionFunnel({data}:{data:{pageViews:number;productViews:number;addToCarts:number;checkoutStarts:number;purchases:number}}){const rows=[{label:"Page view",value:data.pageViews},{label:"Product view",value:data.productViews},{label:"Add to cart",value:data.addToCarts},{label:"Checkout",value:data.checkoutStarts},{label:"Purchase",value:data.purchases}];const max=Math.max(data.pageViews,1);return <Card className="border-[#12372a]/10 shadow-none"><CardHeader><CardTitle className="text-base">Behavior funnel</CardTitle><CardDescription>Customer progression across the storefront</CardDescription></CardHeader><CardContent className="space-y-5">{rows.map((row,index)=>{const pct=row.value/max*100;const previous=index?rows[index-1].value:max;const step=previous?row.value/previous*100:0;return <div key={row.label}><div className="mb-2 flex items-center justify-between text-xs"><span className="font-medium">{row.label}</span><span className="text-muted-foreground">{row.value.toLocaleString()} · {pct.toFixed(1)}% {index>0&&<span className="ml-2 text-[#9a7a16]">step {step.toFixed(1)}%</span>}</span></div><div className="h-2.5 overflow-hidden rounded-full bg-[#12372a]/7"><div className="h-full rounded-full bg-[#12372a] transition-all" style={{width:`${Math.max(pct,row.value?2:0)}%`,opacity:1-index*.12}}/></div></div>})}</CardContent></Card>}
