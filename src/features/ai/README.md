# AI extension boundary

Future AI providers should be added behind typed service interfaces in this folder. UI components and catalog repositories must not call model APIs directly. This keeps product translation, marketing-copy generation, and customer-support agents independently replaceable.
