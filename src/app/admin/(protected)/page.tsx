import Link from "next/link"; import { ArrowUpRight, Eye, ReceiptText, ShoppingBag, TrendingUp } from "lucide-react"; import { PageHeader } from "@/components/admin/page-header"; import { RangeTabs } from "@/components/admin/range-tabs"; import { MetricCard } from "@/components/admin/metric-card"; import { SalesTrendChart, TrafficChart } from "@/components/admin/dashboard-charts"; import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card"; import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table"; import { getDashboardData, parseRange } from "@/features/analytics/dashboard.service";
export default async function Dashboard({searchParams}:{searchParams:Promise<{range?:string}>}){const range=parseRange((await searchParams).range);const data=await getDashboardData(range);return <><PageHeader eyebrow="Overview" title="Dashboard" description="A clear view of your Saudi storefront performance." actions={<RangeTabs range={range} path="/admin"/>}/><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><MetricCard label="Revenue" value={`${data.kpis.revenue.toLocaleString()} SAR`} helper="Confirmed purchase events" icon={TrendingUp} accent/><MetricCard label="Orders" value={String(data.kpis.orders)} helper="Distinct completed orders" icon={ReceiptText}/><MetricCard label="Visitors" value={data.kpis.visitors.toLocaleString()} helper="Unique sessions" icon={Eye}/><MetricCard label="Conversion rate" value={`${data.kpis.conversion.toFixed(2)}%`} helper="Orders divided by visitors" icon={ShoppingBag}/></div><div className="mt-5 grid gap-5 xl:grid-cols-[1.55fr_1fr]"><SalesTrendChart data={data.trend}/><TrafficChart data={data.channels}/></div><Card className="mt-5 border-foreground/10 shadow-none"><CardHeader className="flex-row items-center justify-between"><div><CardTitle className="text-base">Top products</CardTitle><CardDescription>Performance ranked by revenue and views</CardDescription></div><Link href="/admin/products" className="flex items-center gap-1 text-xs font-medium text-primary underline-offset-4 hover:underline">View catalog <ArrowUpRight className="size-3"/></Link></CardHeader><CardContent className="px-0"><Table><TableHeader><TableRow><TableHead className="pl-6">Product</TableHead><TableHead>Views</TableHead><TableHead>Add to cart</TableHead><TableHead>Orders</TableHead><TableHead className="pr-6 text-right">Revenue</TableHead></TableRow></TableHeader><TableBody>{data.topProducts.length?data.topProducts.map(p=><TableRow key={p.id}><TableCell className="pl-6 font-medium"><Link href={`/admin/products/${p.id}/edit`}>{p.name}</Link></TableCell><TableCell>{p.views}</TableCell><TableCell>{p.carts}</TableCell><TableCell>{p.orders}</TableCell><TableCell className="pr-6 text-right font-medium text-[var(--brand-gold)]">{p.revenue.toLocaleString()} SAR</TableCell></TableRow>):<TableRow><TableCell colSpan={5} className="h-28 text-center text-muted-foreground">Product activity will appear as customers browse your store.</TableCell></TableRow>}</TableBody></Table></CardContent></Card></>}
